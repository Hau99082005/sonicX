"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFollowersProfilePublic = exports.getFollowingsProfile = exports.getFllowersProfile = exports.getAutoGeneratedPlaylist = exports.getRecommendByProfile = exports.getPublicPlaylist = exports.getPublicProfile = exports.getPublicUploads = exports.getUploads = exports.updatedFollower = void 0;
const audio_1 = __importDefault(require("../models/audio"));
const history_1 = __importDefault(require("../models/history"));
const playlist_1 = __importDefault(require("../models/playlist"));
const User_1 = __importDefault(require("../models/User"));
const mongoose_1 = require("mongoose");
const helper_1 = require("../utils/helper");
const autoGeneratedPlaylist_1 = __importDefault(require("../models/autoGeneratedPlaylist"));
const updatedFollower = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { profileId } = req.params;
    let status;
    if (!(0, mongoose_1.isValidObjectId)(profileId))
        return res.status(422).json({ error: "Invalid profile id!" });
    const profile = yield User_1.default.findById(profileId);
    if (!profile)
        return res.status(404).json({ error: "Profile not found!" });
    const alreadyFollower = profile.followers.some((id) => id.toString() === req.user.id);
    if (alreadyFollower) {
        yield User_1.default.findByIdAndUpdate(profileId, { $pull: { followers: req.user.id } });
        yield User_1.default.findByIdAndUpdate(req.user.id, { $pull: { followings: profileId } });
        status = "removed";
    }
    else {
        yield User_1.default.findByIdAndUpdate(profileId, { $addToSet: { followers: req.user.id } });
        yield User_1.default.findByIdAndUpdate(req.user.id, { $addToSet: { followings: profileId } });
        status = "added";
    }
    res.status(200).json({ status });
});
exports.updatedFollower = updatedFollower;
const getUploads = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { limit = "80", pageNo = "0" } = req.query;
    const data = yield audio_1.default.find({ owner: req.user.id })
        .skip(parseInt(limit) * parseInt(pageNo))
        .limit(parseInt(limit))
        .sort("-createdAt");
    const audios = data.map(item => {
        var _a;
        return {
            id: item._id,
            title: item.title,
            about: item.about,
            file: item.file.url,
            poster: (_a = item.poster) === null || _a === void 0 ? void 0 : _a.url,
            date: item.createdAt,
            owner: { name: req.user.name, id: req.user.id }
        };
    });
    res.json({ audios });
});
exports.getUploads = getUploads;
const getPublicUploads = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { limit = "80", pageNo = "0" } = req.query;
    const { profileId } = req.params;
    if (!(0, mongoose_1.isValidObjectId)(profileId))
        return res.status(422).json({ error: "Invalid profile Id!" });
    const data = yield audio_1.default.find({ owner: profileId })
        .skip(parseInt(limit) * parseInt(pageNo))
        .limit(parseInt(limit))
        .sort("-createdAt")
        .populate("owner");
    const audios = data.map(item => {
        var _a;
        return {
            id: item._id,
            title: item.title,
            about: item.about,
            file: item.file.url,
            poster: (_a = item.poster) === null || _a === void 0 ? void 0 : _a.url,
            date: item.createdAt,
            owner: { name: item.owner.name, id: item.owner._id }
        };
    });
    res.json({ audios });
});
exports.getPublicUploads = getPublicUploads;
const getPublicProfile = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    var _a;
    const { profileId } = req.params;
    if (!(0, mongoose_1.isValidObjectId)(profileId))
        return res.status(422).json({ error: "Invalid profile Id!" });
    const user = yield User_1.default.findById(profileId);
    if (!user)
        return res.status(404).json({ error: "User not found!" });
    res.json({
        profile: {
            id: user._id,
            name: user.name,
            followers: user.followers.length,
            avatar: (_a = user.avatar) === null || _a === void 0 ? void 0 : _a.url
        }
    });
});
exports.getPublicProfile = getPublicProfile;
const getPublicPlaylist = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { profileId } = req.params;
    const { limit = "80", pageNo = "0" } = req.query;
    if (!(0, mongoose_1.isValidObjectId)(profileId))
        return res.status(422).json({ error: "Invalid profile Id!" });
    const playlist = yield playlist_1.default.find({
        owner: profileId,
        visibility: 'public',
    }).skip(parseInt(limit) * parseInt(pageNo)).limit(parseInt(limit))
        .sort("-createdAt");
    if (!playlist) {
        return res.json({ playlist: [] });
    }
    res.json({
        playlist: playlist.map(item => {
            return {
                id: item._id,
                title: item.title,
                itemsCount: item.items.length,
                visibility: item.visibility,
            };
        })
    });
});
exports.getPublicPlaylist = getPublicPlaylist;
const getRecommendByProfile = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const user = req.user;
    let matchOptions = { $match: { _id: { $exists: true } } };
    if (user) {
        const category = yield (0, helper_1.getUsersPreviosHistory)(req);
        if (category.length) {
            matchOptions = {
                $match: {
                    category: {
                        $in: category
                    }
                }
            };
        }
    }
    const audio = yield audio_1.default.aggregate([
        matchOptions,
        {
            $sort: {
                "likes.count": -1
            }
        },
        { $limit: 10 },
        {
            $lookup: {
                from: "users",
                localField: "owner",
                foreignField: "_id",
                as: "owner"
            }
        },
        { $unwind: "$owner" },
        {
            $project: {
                _id: 0,
                id: "_id",
                title: "$title",
                category: "$category",
                about: "$about",
                file: "$file.url",
                poster: "$poster.url",
                owner: {
                    name: "$owner.name",
                    id: "$owner._id"
                }
            }
        }
    ]);
    res.json({ audio });
});
exports.getRecommendByProfile = getRecommendByProfile;
const getAutoGeneratedPlaylist = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const [result] = yield history_1.default.aggregate([
        { $match: { owner: new mongoose_1.Types.ObjectId(req.user.id) } },
        { $unwind: "$all" },
        {
            $group: {
                _id: "$all.audio", items: {
                    $addToSet: "$all.audio"
                }
            }
        },
        {
            $sample: { size: 20 },
        }, {
            $group: { _id: null, items: { $push: "$_id" } }
        }
    ]);
    const title = "Mixed 30";
    if (result) {
        yield playlist_1.default.updateOne({ owner: req.user.id, title }, {
            $set: {
                title, items: result.items,
                visibility: 'auto'
            }
        }, { upsert: true });
    }
    const category = yield (0, helper_1.getUsersPreviosHistory)(req);
    let matchOptions = { $match: { _id: { $exists: true } } };
    if (category.length) {
        matchOptions = { $match: { $title: { $in: category } } };
    }
    const agpl = yield autoGeneratedPlaylist_1.default.aggregate([
        matchOptions,
        {
            $sample: { size: 4 }
        },
        {
            $project: {
                _id: 0,
                id: "$_id",
                title: "$title",
                itemsCount: { $size: "$items" },
            }
        }
    ]);
    const playlist = yield playlist_1.default.findOne({ owner: req.user.id, title });
    const finalList = agpl.concat({
        id: playlist === null || playlist === void 0 ? void 0 : playlist._id,
        title: playlist === null || playlist === void 0 ? void 0 : playlist.title,
        itemCount: playlist === null || playlist === void 0 ? void 0 : playlist.items.length,
    });
    res.json({ playlist: finalList });
});
exports.getAutoGeneratedPlaylist = getAutoGeneratedPlaylist;
const getFllowersProfile = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { limit = "80", pageNo = "0" } = req.query;
    const [result] = yield User_1.default.aggregate([{
            $match: { _id: new mongoose_1.Types.ObjectId(req.user.id) }
        }, {
            $project: {
                followers: {
                    $slice: ["$followers", parseInt(pageNo) * parseInt(limit), parseInt(limit)]
                }
            }
        }, {
            $unwind: "$followers"
        }, {
            $lookup: {
                from: "users",
                localField: "followers",
                foreignField: "_id",
                as: "userInfo"
            }
        }, {
            $unwind: "$userInfo"
        }, {
            $group: {
                _id: null,
                followers: {
                    $push: {
                        id: "$userInfo._id",
                        name: "$userInfo.name",
                        avatar: "$userInfo.avatar.url",
                    }
                }
            }
        }]);
    if (!result) {
        return res.json({ followers: [] });
    }
    res.json({ followers: result });
});
exports.getFllowersProfile = getFllowersProfile;
const getFollowingsProfile = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { limit = "80", pageNo = "0" } = req.query;
    const [result] = yield User_1.default.aggregate([{
            $match: { _id: new mongoose_1.Types.ObjectId(req.user.id) }
        }, {
            $project: {
                followings: {
                    $slice: ["$followings", parseInt(pageNo) * parseInt(limit), parseInt(limit)]
                }
            }
        }, {
            $unwind: "$followings"
        }, {
            $lookup: {
                from: "users",
                localField: "followings",
                foreignField: "_id",
                as: "userInfo"
            }
        }, {
            $unwind: "$userInfo"
        }, {
            $group: {
                _id: null,
                followings: {
                    $push: {
                        id: "$userInfo._id",
                        name: "$userInfo.name",
                        avatar: "$userInfo.avatar.url",
                    }
                }
            }
        }
    ]);
    if (!result) {
        return res.json({ followings: [] });
    }
    res.json({ followings: result.followings });
});
exports.getFollowingsProfile = getFollowingsProfile;
const getFollowersProfilePublic = (req, res) => __awaiter(void 0, void 0, void 0, function* () {
    const { limit = "80", pageNo = "0" } = req.query;
    const { profileId } = req.params;
    if (!(0, mongoose_1.isValidObjectId)(profileId)) {
        return res.status(422).json({ error: "Invalid profile id!" });
    }
    const [result] = yield User_1.default.aggregate([{
            $match: { _id: new mongoose_1.Types.ObjectId(profileId) }
        }, {
            $project: {
                followers: {
                    $slice: ["$followers", parseInt(pageNo) * parseInt(limit), parseInt(limit)]
                }
            }
        }, {
            $unwind: "$followers"
        }, {
            $lookup: {
                from: "users",
                localField: "followers",
                foreignField: "_id",
                as: "userInfo"
            }
        }, {
            $unwind: "$userInfo"
        }, {
            $group: {
                _id: null,
                followers: {
                    $push: {
                        id: "$userInfo._id",
                        name: "$userInfo.name",
                        avatar: "$userInfo.avatar.url",
                    }
                }
            }
        }]);
    if (!result) {
        return res.json({ followers: [] });
    }
    res.json({ followers: result });
});
exports.getFollowersProfilePublic = getFollowersProfilePublic;
